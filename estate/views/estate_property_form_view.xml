<odoo>
    <record id="property_view_form" model="ir.ui.view">
        <field name="name">estate.property.view.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form string="Estate Property">
                <header>
                    <button name="action_do_sold" type="object" string="Sold"
                        invisible="state in ['sold']" />
                    <button name="action_do_cancel" type="object" string="Cancel"
                        invisible="state in ['cancelled']" />
                    <field name="state" statusbar_clickable="True" widget="statusbar"
                        statusbar_visible="new,offer_accepted,offer_recieved,sold" />
                </header>
                <sheet>
                    <h1>
                        <field name="name" />
                    </h1>
                    <group>
                        <field name="tag_ids" widget="many2many_tags"
                            options="{'color_field': 'color'}" />
                        <group>
                            <field name="property_type_id" widget="many2one_property_type_id"
                                options="{'no_create':true,'no_create_edit':true}" />
                            <field name="postcode" />
                            <field name="date_availability" />
                        </group>
                        <group>
                            <field name="expected_price" />
                            <field name="best_price" />
                            <field name="selling_price" />
                        </group>
                    </group>

                    <notebook>
                        <page string="Description">
                            <group>
                                <field name="description" />
                            </group>
                            <group>
                                <field name="bedrooms" />
                                <field name="living_area" />
                                <field name="facades" />
                                <field name="garage" />
                                <field name="garden" />
                                <field name="garden_area" invisible="not garden" />
                                <field name="garden_orientation" invisible="not garden" />
                                <!-- <field name="state"/> -->
                                <field name="total_area" />
                            </group>
                        </page>
                        <page string="Offers">
                            <field name="offer_ids" readonly="state in ['offer_accepted','sold','cancelled']" />
                        </page>
                        <page string="Other Info">
                            <group>
                                <field name="user_id" />
                            </group>
                            <group>
                                <field name="partner_id" />
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>


</odoo>