
<odoo>
    <!-- Action Handler -->
    <record id="estate_property_action" model="ir.actions.act_window">
        <field name="name">Property</field>
        <field name="res_model">estate_property</field>
        <field name="view_mode">list,form,kanban</field>
        <field name="context">{'search_default_estate_state': 1}</field>

    </record>

    <!-- List View -->
    <record id="estate_property_view_list" model="ir.ui.view">
        <field name="name">estate.property.list</field>
        <field name="model">estate_property</field>
        <field name="arch" type="xml">
            <list string="Estates" decoration-success="offers_ids" decoration-bf="state == 'offer_accepted'">
                <field name="name" string="Estate"/>
                <field name="postcode"/>
                <field name="bedrooms"/>
                <field name="living_area"/>
                <field name="expected_price"/>
                <field name="selling_price"/>
                <field name="date_availability" string="Available From" optional="hidden"/>
            </list>
        </field>
    </record>

    <!-- Kanban View -->
    <record id="estate_property_view_kanban" model="ir.ui.view">
        <field name="name">estate.property.list</field>
        <field name="model">estate_property</field>
        <field name="arch" type="xml">
            <kanban>
                <t t-name="card">
                    <div>
                        <field name="name" class="text-truncate"/>
                        <field name="expected_price" class="text-truncate"/>

                        <div t-if="record.state == 'offer_received'">
                            <field name="best_price" class="text-truncate"/>
                        </div>
                        <div t-if="record.state == 'offer_accepted' or record.state == 'Sold'">
                            <field name="selling_price" class="text-truncate"/>
                        </div>
                    </div>
                </t>
            </kanban>
        </field>
    </record>


    <!-- Form View -->
    <record id="estate_property_form" model="ir.ui.view">
        <field name="name">estate.property.form</field>
        <field name="model">estate_property</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="action_sell_property" type="object" string="Sold" class="btn-primary" invisible="state in ('sold','cancelled')"/>
                    <button name="action_cancel_property" type="object" string="Cancel" class="btn-secondary" invisible="state in ('sold','cancelled')"/>
                    <field name="state" widget="statusbar" statusbar_visible="new,offer_received,offer_accepted,sold"/>
                </header>
                <sheet>
                    <h1><field name="name"></field></h1>
                    <group>
                        <group>
                            <field name="postcode"/>
                            <field name="date_availability" string="Available From"/>
                        </group>
                        <group>
                            <field name="expected_price"/>
                            <field name="selling_price"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Description">
                            <group>
                                <field name="description"/>
                            </group>
                            <group>
                                <group>
                                    <field name="bedrooms"/>
                                    <field name="living_area"/>
                                    <field name="facades"/>
                                    <field name="has_garage"/>
                                    <field name="has_garden"/>
                                    <field name="garden_area" string="Garden Area (sqm)" invisible="not has_garden"/>
                                    <field name="garden_orientation" invisible="not has_garden"/>
                                    <field name="total_area"/>
                                </group>
                                <group>
                                    <field name="property_type_id" options="{'no_create': true, 'no_open': true}"/>
                                    <field name="property_tags_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
                                    <field name="best_price"/>
                                </group>
                            </group>
                        </page>
                        <page string="Offers">
                            <group>
                                <field name="offers_ids" readonly="state in ('sold', 'offer_accepted')"/>
                            </group>
                        </page>
                        <page string="Other Infos">
                            <group>
                                <field name="buyer_id"/>
                                <field name="salesperson_id"/>
                                <field name="state"/>
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- SearchBar -->
     <record id="estate_property_search" model="ir.ui.view">
        <field name="name">estate.property.search</field>
        <field name="model">estate_property</field>
        <field name="arch" type="xml">
            <search string="Search properties">
                <field string="Title" name="name"/>
                <field name="postcode"/>
                <field name="expected_price"/>
                <field name="bedrooms"/>
                <field string="Living Area (sqm)" name="living_area" filter_domain="[('living_area', '&gt;', self)]"/>
                <field name="facades"/>

                <filter name="state" string="Available" domain="[('state', 'in', ('new', 'offer_received'))]"></filter>
                <group>
                    <filter string="PostCode" name="postcode" context="{'group_by':'postcode'}"/>
                </group>
            </search>
        </field>
    </record>
</odoo>
