<?xml version="1.0" encoding="utf-8"?>
<data>
    <record id="estate_property_action" model="ir.actions.act_window">
        <field name="name">Estate Property</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">list,form</field>
        <field name="context">{'search_default_available': True}</field>
    </record>

    <record id="estate_property_search" model="ir.ui.view">
        <field name="name">Search your Estate</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search string="Search...">
                <field name="name" string="Title" />
                <field name="postcode" />
                <field name="expected_price" />
                <field name="bedrooms" />
                <field name="living_area" string="Living Area (sqm)" filter_domain="[('living_area', '>=', self)]" />
                <separator />
                <filter name="available" string="Available"
                    domain="['|', ('state', '=', 'new'), ('state','=', 'offer received')]" />
                <group expand="1" string="Group By">
                    <filter name="postcode" context="{'group_by':'postcode'}" />
                </group>
            </search>
        </field>
    </record>

    <record id="estate_property_list_view" model="ir.ui.view">
        <field name="name">estate.property.tree</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <tree string="Estate Properties" open_form_view="True" editable="bottom" decoration-success="state == 'offer received' or state == 'offer accepted'" decoration-bf="state == 'offer accepted'" decoration-muted="state == 'sold'">
                <field name="name" />
                <field name="property_type_id" />
                <field name="postcode" />
                <field name="tag_ids" widget="many2many_tags" />
                <field name="bedrooms" />
                <field name="living_area" />
                <field name="expected_price" />
                <field name="selling_price" />
                <field name="date_availability" optional="hide" />
                <field name="state" column_invisible="True" />
                <!-- <field name="facades" />
                <field name="garden_area" />
                <field name="garden_orientation" /> -->
            </tree>
        </field>
    </record>

    <record id="estate_property_view_form" model="ir.ui.view">
        <field name="name">estate.property.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form string="Estate Property">
                <header>
                    <button name="action_sell_property" type="object" string="Sell"
                        invisible="state == 'sold'" />
                    <button name="action_cancel_property" type="object" string="Cancel"
                        invisible="state == 'sold'" />
                    <field name="state" widget="statusbar"
                        statusbar_visible="new,offer received, offer accepted,sold" />
                </header>
                <sheet>
                    <h1 class="mb32">
                        <field name="name" class="mb16" />
                    </h1>
                    <group>
                        <field class="mb16" name="tag_ids" widget="many2many_tags"
                            options="{'color_field': 'color'}" />
                        <group>
                            <field string="Status" name="state" />
                            <field name="property_type_id" string="Property Type"
                                options="{'no_create': true}" />
                            <field name="postcode" />
                            <field string="Available From" name="date_availability" />
                        </group>
                        <group>
                            <field name="best_price" />
                            <field name="expected_price" />
                            <field name="selling_price" />
                        </group>
                    </group>
                    <notebook>
                        <page string="Description">
                            <group>
                                <field name="active" class="mb16" />
                                <field name="description" class="mb16" />
                                <field name="bedrooms" class="mb16" />
                                <field string="Living Area (sqm)" name="living_area" class="mb16" />
                                <field name="facades" class="mb16" />
                                <field name="garage" class="mb16" />
                                <field name="state" invisible="True" />
                                
                                <field name="garden" class="mb16" />
                                <field string="Garden Area (sqm)" name="garden_area"
                                    invisible="garden == False" class="mb16" />
                                <field name="garden_orientation"
                                    invisible="garden == False" class="mb16" />

                                <field string="Total Area (sqm)" name="total_area" class="mb16" />
                            </group>
                        </page>
                        <page string="Offers">
                            <field name="offer_ids">
                                <tree editable="bottom" decoration-success="status == 'accepted'" decoration-danger="status == 'refused'">
                                    <field name="price" />
                                    <field name="partner_id" string="Partner" />
                                    <field name="validity" string="Validity (days)" />
                                    <field name="date_deadline" string="Deadline" />

                                    <button name="action_accept_offer"
                                        type="object"
                                        title="Accept"
                                        icon="fa-check"
                                        invisible="status == 'accepted' or status == 'refused'" />
                                    <button name="action_reject_offer"
                                        type="object"
                                        title="Refuse"
                                        icon="fa-times"
                                        invisible="status == 'accepted' or status == 'refused'" 
                                        />
                                    <field name="status" column_invisible="True" />
                                </tree>
                            </field>
                        </page>
                        <page string="Other Info">
                            <group>
                                <field name='salesman' class='mb16'
                                    invisible="state in ['offer accepted', 'sold', 'cancelled']" />
                                <field name='buyer' class='mb16'
                                    invisible="state in ['offer accepted', 'sold', 'cancelled']" />
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>
</data>