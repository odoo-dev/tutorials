<?xml version="1.0"?>
<odoo>
    <record id="edit_project_inherited" model="ir.ui.view">
        <field name="name">project.project.form.inherit</field>
        <field name="model">project.project</field>
        <field name="inherit_id" ref="project.edit_project"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='date_start']" position="after">
                <button name="open_cut_off_wizard" type="object" string="Recognise the invoices" invisible="not is_recognition_required" icon="fa-refresh"/>
            </xpath>
            <xpath expr="//form/header" position="inside">
                <div class="alert alert-warning" role="alert" invisible="not is_recognition_required">
                    <p>
                        You have still journal items to be recognised from <b><field name="date_start" readonly="1"/></b>
                    </p>
                </div>
            </xpath>
        </field>
    </record>
</odoo>
