<?xml version="1.0" encoding="UTF-8"?>

<odoo>
    <template id="product_template_inherit_purchase" inherit_id="website_sale.product">
        <xpath expr="//div[@id='o_wsale_cta_wrapper']/div[@id='add_to_cart_wrap']/a/i" position="replace">
            <i class="fa fa-shopping-cart me-2"/>
            Buy
        </xpath>

        <xpath expr="//div[@id='o_wsale_cta_wrapper']" position="after">
            <div id="o_wsell_cta_wrapper" class="d-flex flex-wrap align-items-center">
                <div t-attf-class="css_quantity input-group {{'d-none' if combination_info['prevent_zero_price_sale'] else 'd-inline-flex'}} me-2 mb-2 align-middle" contenteditable="false">
                    <a href="#" class="btn btn-link js_remove_sell_json" aria-label="Remove one" title="Remove one">
                        <i class="fa fa-minus"/>
                    </a>
                    <input type="text" class="form-control quantity text-center" data-min="1" name="sell_qty" t-att-value="1"/>
                    <a href="#" class="btn btn-link float_left js_add_sell_json" aria-label="Add one" title="Add one">
                        <i class="fa fa-plus"/>
                    </a>
                </div>

                <div id="sell_to_cart_wrap" t-attf-class="{{'d-none' if combination_info['prevent_zero_price_sale'] else 'd-inline-flex'}} align-items-center mb-2 me-auto">
                    <a role="button" id="sell_to_cart" class="btn btn-primary js_sell_product flex-grow-1" href="#">
                        <i class="fa fa-shopping-cart me-2"/>
                        Sell
                    </a>
                </div>
            </div>
        </xpath>
    </template>
</odoo>
